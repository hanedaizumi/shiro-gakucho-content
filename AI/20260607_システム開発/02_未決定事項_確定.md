# 未決定事項 — 確定内容（2026-06-07）

実装時のデフォルト決定。後から設定画面で変更可能。

| # | 項目 | 決定 |
|---|---|---|
| 1 | **LLMプロバイダ** | OpenAI をデフォルト。環境変数 `LLM_PROVIDER`（openai / anthropic / gemini）で切替 |
| 2 | **X API** | Phase 1 は手動ペースト欄。Phase 2 で `X_API_BEARER_TOKEN` 設定時に自動取得 |
| 3 | **YouTube参照チャンネル** | 設定画面で管理。初期値: 検索クエリ「ビットコイン チャート分析」「BTC technical analysis」 |
| 4 | **出力連携** | ダウンロード + ローカル自動保存（`output/reports/`, `output/scripts/`）。シロ学長テクニカルフォルダは `TECHNICAL_OUTPUT_PATH` で任意指定 |
| 5 | **台本番号 N** | DB `ScriptHistory` の最大値 + 1 で自動インクリメント。生成時に手動上書き可能 |

## 必要な環境変数

```env
DATABASE_URL=postgresql://shiro:shiro_dev_password@localhost:5432/shiro_ai_dev?schema=public
LLM_PROVIDER=openai
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
COINMARKETCAP_API_KEY=
YOUTUBE_API_KEY=
X_API_BEARER_TOKEN=
TECHNICAL_WORKSPACE_PATH=../シロ学長テクニカル
TECHNICAL_OUTPUT_PATH=
```
